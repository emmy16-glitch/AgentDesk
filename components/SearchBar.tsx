import { Search, Filter, ChevronDown, X } from "lucide-react";
import { categories } from "@/data/categories";

export default function SearchBar({ activeCategory, setActiveCategory, query, setQuery }: { activeCategory: string; setActiveCategory: (c: string) => void; query: string; setQuery: (q: string) => void }) {
  return <section className="search-area" aria-label="Agent marketplace filters"><div className="search-row"><div className="agent-search"><Search size={19} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search registered BSC agents..." aria-label="Search agents" />{query && <button type="button" aria-label="Clear search" onClick={() => setQuery("")}><X size={15} /></button>}</div><button type="button" className="filter-button" title="Additional registry filters are coming in Phase 1"><Filter size={18} /> Filter</button></div><div className="filter-row"><div className="category-scroll">{categories.map((cat) => <button type="button" key={cat} onClick={() => setActiveCategory(cat)} className={activeCategory === cat ? "active" : ""}>{cat}</button>)}</div><button type="button" className="sort-button" title="Current discovery order comes from source search relevance"><span>Source</span> ERC-8004 <ChevronDown size={14} /></button></div></section>;
}
